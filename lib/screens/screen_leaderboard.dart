import 'package:flutter/material.dart';

class ScreenLeaderboard extends StatelessWidget {
  const ScreenLeaderboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}